export type BookCardProps = { // en ts tengo que definir los tipos de las props
    // que parametros esperan, si son opcionales o no, que tipo de dato es cada uno
    title: string;
    author: string;
    genre: string;
    synopsis: string;
    yearPublished: number;
};
